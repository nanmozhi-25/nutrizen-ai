# URL of your deployed (or local) backend
# Local development:
REACT_APP_API_URL=http://localhost:5000

# After deploying the backend (e.g. Render), replace with:
# REACT_APP_API_URL=https://your-backend.onrender.com
